suite: Fail when image.name is missing
tests:
  - it: should fail if image.name is not set
    values: 
      - |
        image:
          tag: latest
    asserts:
      - failedTemplate:
          errorMessage: ".*image.name.*"
